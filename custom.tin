#FUNC {tag_end_minimap} {
	#if {$autoscan}	{
		scan
	}
}

#FUNC {tag_start_roomchars} {
	#line log {$PANE_PATH/roomchars} {------<179>[ $GMCP[ROOM][INFO][name] ]<099>}
}
  
#FUNC {tag_end_roomchars} {
	#line log {$PANE_PATH/roomchars} {\n}
}

#READ alias.tin 
